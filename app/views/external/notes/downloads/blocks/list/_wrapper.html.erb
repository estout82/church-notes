<% nested ||= false %>

<% if style == "unordered" %>
  <ul class="list-inside list-disc <%= nested ? "ml-4" : "my-4" %>">
    <%= yield %>
  </ul>
<% else %>
  <ol>
    <%= yield %>
  </ol>
<% end %>
